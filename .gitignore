# secrets
.env
.env.local
.env.*.local

# os / editor junk
desktop.ini
.DS_Store
Thumbs.db
*.swp

# build & deps
node_modules/
dist/
.vercel/
.netlify/

# stray downloads
*.deb

# supabase cli state — project ref and pooler host, not for the repository
supabase/.temp/

# anything that ever looks like a private key or a database password
*.pem
*.key
*service_role*
*service-role*
